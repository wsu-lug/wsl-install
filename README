# VSCode GZip Workaround
This script will help you run VSCode in WSL if you run into the error about GZip.

Use "vscode-git-workaround.sh" in WSL following the steps below.

Usage

0. Please install Visual Studio Code first: https://code.visualstudio.com/download

1. Download this as a ZIP file and unzip it into your Downloads folder (in Windows).

2. Start WSL from your Download folder from Powershell/CMD. "cd Downloads" && "wsl"

3. In Ubuntu, run "sed -i 's/\r$//' vscode-git-workaround.sh" to fix a format error.

4. In Ubuntu, run the script "vscode-git-workaround.sh"
